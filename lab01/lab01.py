from matrix import Matrix
from perceptron import Perceptron


def main(): pass
    # p = Perceptron()
    # p.add_layer(num_inputs=3, num_outputs=2)
    # p.add_layer(num_outputs=5)
    # p.add_layer(num_outputs=4)
    #
    # in_vector = [1, 2, 3]
    # out_vector = [1, 0, 0, 1]
    # p.learn(in_vector, out_vector)


if __name__ == "__main__":
    main()
